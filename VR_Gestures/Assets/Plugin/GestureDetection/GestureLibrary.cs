using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Interface for a library of gestures that all _____Gestures libraries should implement
/// <br></br><b>Different libraries may contain the same gesture as different names</b>
/// </summary>
public class GestureLibrary : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
